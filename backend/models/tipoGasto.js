/*import mongoose, { Schema} from 'mongoose';

const tipoGastoSchema = new Schema({
    nombreGasto: {type:String, maxlength:50,required:true},
    gastoTipo: {type: String, maxlength:90},
    estadoTipo: {type: Number,default:1}
});

const tipoGasto = mongoose.model('tipoGasto',tipoGastoSchema);
export default tipoGasto;
*/